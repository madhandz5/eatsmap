package com.eatsmap.module.memberNoticeHistory;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberNoticeHistoryRepository extends JpaRepository<MemberNoticeHistory, Long> {
}
